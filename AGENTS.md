before answering any request, mandatory:

pull main / branch, read repo readme, docs, invariants, and code overview, then check the 'goldmine' obsidian vault using kb tools, and `gh project` kanban for status updates and task timeline/priorities (create if not found any of the above), then answer the user prompt.
