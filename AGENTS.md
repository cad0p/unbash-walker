before answering any request, mandatory:

pull main / branch, read repo readme, docs, invariants, and code overview, then check the `gh project` kanban issues for status updates and task timeline/priorities (create if not found any of the above), then answer the user prompt.
